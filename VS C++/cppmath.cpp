#include <iostream>
#include <cmath>
using namespace std;

int main(){

    double x = 3;
    double y = 1;
    double z;

    // Prints the bigger double of the set.
    // z = max (x , y);
    // cout << z;
    
    // Prints the smallest double of the set.
    // z = min (x , y);
    // cout << z;

    // Raises the First number to the power of the second.
    //  z = pow(2 , 4);
    // cout << z;

    // The Square Root of the Number.
    // z = sqrt(9);
    // cout << z;

    // The Absolute Value of the Number.
    // z = abs(-3);
    // cout << z;

    // Rounds the Number.
    // z = round(3.55);
    // cout << z;

    // Rounds Up.
    // z = ceil(3.55);
    // cout << z;

    // Rounds Down.
    // z = floor(3.55);
    // cout << z;
    return 0;
}